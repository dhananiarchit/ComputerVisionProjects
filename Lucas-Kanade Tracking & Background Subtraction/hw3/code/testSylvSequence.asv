% load frames

load sylvbases.mat
load sylvseq.mat
% implementation
It = frames(:,:,1);
rect =  [102, 62, 156, 108];
for i = 2:size(frames,3)
    It1 = frames(:,:,i);
    [u,v] = LucasKanadeBasis(It, It1, rect, bases);
    
    imshow(frames(:,:,i));
end












% save the rects
% save sylvseqrects.mat rects